require File.expand_path(File.dirname(__FILE__) + '/../../spec/spec_helper')

ENV['RACK_ENV'] ||= 'test'
<%= module_name %>.set :environment, :test

require 'capybara/cucumber'
Capybara.app = <%= module_name %>

module MyWorld
  def app
    @app ||= Rack::Builder.new do
      run <%= module_name %>
    end
  end
  include Rack::Test::Methods
end

World(MyWorld)
